{
  "name": "@casework/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Casework MCP server: catalog, cases, evidence, outreach",
  "scripts": {
    "api": "tsx src/entrypoints/http.ts",
    "attribute": "tsx src/entrypoints/attribute.ts",
    "build:cases": "tsx src/entrypoints/build.ts",
    "mcp:http": "tsx src/entrypoints/mcpHttp.ts",
    "reset": "tsx src/entrypoints/reset.ts",
    "start": "tsx src/entrypoints/stdio.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "better-sqlite3": "^13.0.2",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@truefoundry/trueforge-core": "^0.1.4",
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.10.0",
    "vitest": "^3.0.0"
  }
}
