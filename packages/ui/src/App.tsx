// Shell only. The queue and case routes land on 08-28, per docs/SPEC.md section 11:
// React routes own the screens, OpenUI owns the in-chat summary and approval prompt.
export function App() {
  return <main>Casework</main>
}
